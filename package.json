{
  "name": "hello",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "test": "echo \"add a test runner\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
