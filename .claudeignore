# graphify writes these into the workspace on every extract/update.
# Unignored, each write invalidates Claude Code's prompt cache and forces a
# full re-upload at cache-write rates on the next turn.
graph.json
graphify-out/
