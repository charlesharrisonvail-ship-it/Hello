# Memory

One fact per file. The frontmatter IS the index -- there is no separate list to
keep in sync, because a hand-maintained index drifts and then silently hides
memories that exist.

    ---
    name: short-kebab-slug
    description: one line saying when this is relevant
    ---

    The fact itself.

Search with `python .claude/continuity/recall.py <terms>`.
